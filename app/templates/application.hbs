{{#if session.isAuthenticated}}
<header id="main-header">
Logged in as {{ session.data.authenticated.user.displayName }}
<button class="logout-button" {{action "logout"}}>Sign out</button>
<br>
</header>
<ul>
    <li>
        <LinkTo @route="index">Search</LinkTo>
    </li>
    <li>
        <LinkTo @route="favorites">Favorites</LinkTo>
    </li>
</ul>
{{outlet}}
{{else}}
<header id="main-header">
  <button class="login-button" {{action "login"}}>Sign in with Google</button>
</header>
{{/if}}
