query searchFunction($term: String!, $location: String!){
    search(term: $term,
            location: $location,
            limit: 5){
               total
               business{
                   name
                   url
                   id
               }
    }
}
