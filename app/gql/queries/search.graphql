query search($term: String, $location: String, $price: String,) {
    search(
        term: $term,
        location: $location,
        price: $price,
        limit: 10
    ){
    total
    business {
        name
        url
        id
        }
    }
}
